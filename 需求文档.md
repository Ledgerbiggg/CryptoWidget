# CryptoWidget 需求文档

> 桌面端加密货币实时行情悬浮组件。技术栈：WPF (.NET)，C# 重写（替代原有 Go 版本）。
> 目标：零配置傻瓜式启动，配置内置在软件内由用户手动修改，无需外部配置文件。
> 程序图标（窗口 + 托盘）统一使用**比特币 ₿ 图标**。
> OKX WebSocket 调用逻辑参考 ledger-service 现有实现（含代理支持）。

---

## 1. 总体目标

- 一个轻量、常驻桌面的加密货币行情悬浮卡片。
- 启动即用：无需任何配置文件，默认订阅 BTC，开箱即看行情。
- 所有配置都在软件内部完成（界面内手配），不依赖外部 `.json/.yaml/.toml` 等配置文件。
- 性能优于原 Go 组件方案，行情刷新流畅、资源占用低。

---

## 2. 主界面（桌面悬浮卡片）

### 2.1 窗口形态

- **无边框、圆角**：现代感卡片样式，半透明背景。
- **无任务栏图标**：不出现在系统任务栏，仅通过托盘/拖拽交互。
- **可拖动**：在卡片空白处按住鼠标即可拖动窗口位置。

### 2.2 钉（置顶）按钮 —— 仿微信

> 改动点：去掉"始终置顶"的默认行为，改为**用户手动钉住才置顶**（参照微信聊天窗口的「钉」按钮）。

- 卡片右上角提供**钉按钮（📌 / Pin）**。
- **未钉住（默认）**：`Topmost = false`，窗口不强制在最上层，可被子窗口遮挡（符合"国外习惯"，不打扰用户）。
- **已钉住**：`Topmost = true`，窗口始终显示在最上层。
- 钉状态在界面上有明显视觉区分（如高亮 / 按下态），并随配置持久化保存。

### 2.3 内容布局

- 单行显示已订阅的币种（横向排列）。**窗口宽度随币种数自动扩展**（每个币种单元固定宽度），始终显示全部币种，不做内部滚动。
- 每个币种单元包含以下子元素（各元素是否显示由配置开关控制）：
  - **状态圆点**：绿 = 已连行情；红 = 断线。
  - **币种图标**（可选显示）。
  - **名称**（如 BTC）。
  - **价格**（如 67,234.5）。
  - **涨跌幅**（如 +1.23% / -0.45%）。

### 2.4 视觉规则

- **涨跌幅着色**：按国外习惯，**绿涨红跌**。
- **价格着色**：可配置「价格按涨跌着色」开关；开启后价格随涨跌变绿/红，关闭则固定白色。
- **币种图标**：从 OKX 官方图标 CDN 远程加载，加载失败回退显示**首字母色块**（圆形底色 + 币种首字母）。
- **价格显示格式**：默认**原样显示 OKX 推送的价格字符串**（不加工、不加千分位分隔符）；每个币种**可单独配置小数位数**，配置后按该位数格式化（四舍五入）。
- **关闭按钮（×）**：右上角浅灰色 `×`，点击后**隐藏到系统托盘常驻**（不直接退出进程，参照微信关闭窗口的行为）。真正退出走托盘菜单。

---

## 3. 系统托盘

- 常驻托盘图标（比特币 ₿ 图标）。
- **左键单击托盘图标** → 呼出 / 恢复显示主卡片（卡片被 `×` 隐藏后靠它找回）。
- 右键菜单仅包含一项：**`Quit`** —— 退出程序（真正结束进程）。
- 关闭主窗口 `×` → 仅隐藏到托盘（不退出）；托盘 `Quit` → 退出。
- **单实例**：重复启动时唤醒已有实例并显示主卡片，不重复开窗。

---

## 4. 配置（内置，默认只订阅 BTC）

所有配置项均在软件内部定义，用户通过界面手动修改，不读写外部配置文件。

### 4.1 币种列表

- 默认：`[BTC]`。
- 支持增删：**输入币种代码（如 `ETH`）自动拼成 `ETH-USDT`**，也允许直接输入完整交易对（如 `PEPE-USDT`）。
- 每个币种可**单独配置价格小数位数**（可选，不配置则原样显示 OKX 返回的价格）。
- 保存后即时生效（重新订阅行情）。

### 4.2 显示开关（布尔值）

| 配置项 | 说明 | 默认 |
| --- | --- | --- |
| 显示图标 | 币种图标是否显示 | 开 |
| 显示名称 | 币种名称是否显示 | 开 |
| 显示价格 | 价格是否显示 | 开 |
| 显示涨跌幅 | 涨跌幅是否显示 | 开 |
| 价格按涨跌着色 | 价格随涨跌变色 | 关（价格固定白色） |
| 开机自启 | 系统登录时自动启动 | 关 |

> 注：已按需求**取消「字体大小」配置项**。

### 4.3 窗口行为状态（自动持久化，非手动配置）

- **钉住状态（IsPinned）**：是否始终置顶，默认未钉住。
- **窗口位置**：首次启动默认屏幕右上角；拖动后保存位置，下次启动恢复。

---

## 5. 数据来源与连接

- **数据源**：OKX 公共 WebSocket 行情接口（无需鉴权）。
- **订阅方式**：实时订阅所配置币种的行情（`tickers` 频道，最新成交价 `last` + 24h 涨跌幅，涨跌幅由 `(last - open24h) / open24h × 100` 计算）。
- **连接地址**：主节点 `wss://ws.okx.com:8443/ws/v5/public`；主节点不可用时自动切换到备用节点 `wss://wsaws.okx.com:8443/ws/v5/public`（AWS 节点）。
- **代理支持**（参考 ledger-service 实现）：默认走系统代理 / 环境变量（`HTTP_PROXY`/`HTTPS_PROXY`）；也可在设置中配置显式代理地址（如 `http://127.0.0.1:7890`）。
- **自动重连**：网络断开或连接异常时自动重连，指数退避（1s 起步，×2 递增，封顶 30s），重连成功自动恢复订阅。
- **断线提示**：状态圆点变红，直观提示当前处于断线状态。
- **性能要求**：增量更新 UI，避免整体重绘；行情推送频率高时仍需保持流畅。

---

## 6. 交互汇总

| 操作 | 行为 |
| --- | --- |
| 拖动卡片空白区域 | 移动窗口 |
| 点击右上角「钉」📌 | 切换置顶（钉住=始终最前；未钉=普通窗口） |
| 点击右上角「设置」⚙ | 打开设置窗口（币种增删、显示开关、开机自启、代理、小数位） |
| 点击右上角 `×` | 隐藏到托盘（不退出进程） |
| 左键单击托盘图标 | 呼出 / 恢复显示主卡片 |
| 右键托盘图标 | 弹出菜单，仅 `Quit` |
| 修改币种列表 | 立即重新订阅 |
| 切换显示开关 | 界面即时更新 |
| 重复启动 exe | 唤醒已有实例并显示主卡片（单实例） |

---

## 7. 技术约束

- 框架：WPF + C#（.NET，建议 net8.0-windows / net9.0-windows）。
- 配置持久化：使用 `Application.UserAppDataPath` / `IsolatedStorage` / 注册表等应用内存储（**不生成可读的外部配置文件**，除非用户另行要求）。
- 不依赖外部配置文件即可运行。
- 开机自启通过注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 实现。

---

## 8. 实现参考（仿 AIMux 项目结构，不模仿布局）

> 现有项目 `d:/1-projects/dotnet-projtct/AIMux` 是一个写得较好的 WPF 项目，本项目**复用其代码组织结构**，但**不模仿其界面布局**。关键可借鉴点：

### 8.1 分层与依赖注入（Prism + Unity）

- 采用 **PrismApplication** 作为 `App` 基类，使用 `IContainerRegistry` 做依赖注入。
- 分层参考：
  - `CryptoWidget.Shell`：Views（含 `MainWindow`）、ViewModels（`MainViewModel`、`SettingsViewModel`）、App.xaml。
  - `CryptoWidget.Services`：业务服务，按 `IService/` 接口 + `Service/` 实现拆分（如 `ITrayService`/`TrayService`、`IMarketService`/`MarketService`）。
  - `CryptoWidget.Common`：配置持久化（`ConfigService`）、日志（`LoggerHelper`）、模型。
  - `CryptoWidget.Models`：数据模型（如 `Ticker`、`CoinConfig`）。
- 服务以 `RegisterSingleton` 注册；窗口 `MainWindow` 用 `RegisterSingleton`，设置窗口用瞬态 `Register`（关闭后不可复用，ViewModel 保持 Singleton）。

### 8.2 托盘服务（仿 AIMux `TrayService`）

- 基于 `System.Windows.Forms.NotifyIcon` 实现 `ITrayService`。
- 事件模型：`OpenRequested`（左键单击呼出）、`ShowRequested`（右键显示/隐藏切换）、`ExitRequested`（右键 Quit）。
- 托盘菜单仅保留 `Quit`（本需求不需要"显示/隐藏"条目，可简化为仅 Quit，但保留事件供扩展）。

### 8.3 关闭到托盘（仿 AIMux `MainWindow`）

- `MainWindow` 维护 `_closingToTray = true`，`Closing` 事件中 `e.Cancel = true; Hide();`，即点 `×` 仅隐藏到托盘。
- 托盘 `Quit` → 置 `_closingToTray = false` → `TrayService.Hide()` → `Application.Current.Shutdown()` 真正退出。
- 这一套"关闭=最小化到托盘、Quit=退出"逻辑直接沿用。

### 8.4 全局异常兜底（仿 AIMux `App.xaml.cs`）

- `DispatcherUnhandledException` + `AppDomain.CurrentDomain.UnhandledException` 兜底，避免静默闪退；可写崩溃日志到桌面（参考 `DumpCrash`）。
- 单实例保护（Mutex）可参考，避免重复启动多个悬浮窗。

### 8.5 配置持久化（仿 AIMux `ConfigService`）

- 配置存入应用数据目录（`%AppData%\CryptoWidget\settings.json`，对用户透明，界面内手配）或注册表（开机自启）；读写均 try/catch 兜底，绝不因配置问题导致启动失败。
- 首次启动若无配置则生成默认配置（默认订阅 BTC、各显示开关默认开、未钉住、窗口默认右上角）。

### 8.6 OKX 行情接入（参考 ledger-service）

- 订阅协议与数据字段解析参考 `ledger-service` 的 `t_okx_ticker.go`（`tickers` 频道、`last`/`open24h` 字段、`subscribe` 消息格式）。
- 代理逻辑对齐：默认走系统代理/环境变量，显式代理优先。
- 断线重连采用指数退避，`reconnecting` 标志防止并发重连。

### 8.7 程序图标

- 窗口图标与托盘图标统一使用**比特币 ₿ 图标**，作为内嵌资源打包（`Assets/`），加载失败回退系统图标，绝不因缺资源崩溃。

---

## 9. 验收标准

1. 双击启动后无需任何配置即可看到 BTC 实时行情（默认未钉住）。
2. 点击「钉」按钮后窗口置顶，再次点击取消置顶；钉状态关闭重启后保留。
3. 断网后圆点变红，恢复后自动重连并变绿。
4. 可通过界面增删币种，改动即时生效。
5. 所有显示开关可独立切换且即时生效；涨跌幅/价格着色遵循绿涨红跌。
6. 点击 `×` 仅隐藏到托盘；托盘右键 `Quit` 才退出。
7. 开机自启开关可正常开关。
8. 长时间运行内存/CPU 占用稳定。
9. 左键单击托盘可呼出被 `×` 隐藏的卡片；重复启动 exe 只唤醒不重复开窗。
10. 程序图标（窗口 + 托盘）为比特币 ₿ 图标。
